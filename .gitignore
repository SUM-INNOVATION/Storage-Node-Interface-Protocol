# Rust build output
/target/
target/

# Cargo lock (library projects — optional, but this is a binary workspace so you may want to commit it)
# Cargo.lock

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment / secrets — never commit signing material in any form.
.env
*.pem
*.key
*.seed
*.seed.hex
keys/
# Generated WS2b test seeds (e2e-helper generate-e2e-keys output dirs).
# These dirs hold OsRng-generated seeds that get funded on the local
# mirror; treat as signing material and keep out of git.
e2e_keys/
extra-alloc.json
extra-alloc.local.json

# OS
Thumbs.db

# Ad-hoc logs
*.log
SNIP_*.txt

# Internal Reference
.claude/

# Generated mobile bindings (built by scripts/build-xcframework.sh)
bindings/SnipCore/SnipCore.xcframework/
bindings/SnipCore/Sources/
bindings/SnipCore/*.zip
