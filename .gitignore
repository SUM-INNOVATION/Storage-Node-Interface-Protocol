# Rust build output
/target/
target/

# Cargo lock (library projects — optional, but this is a binary workspace so you may want to commit it)
# Cargo.lock

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment / secrets — never commit signing material in any form.
.env
*.pem
*.key
*.seed
*.seed.hex
keys/

# OS
Thumbs.db

# Ad-hoc logs
*.log
SNIP_*.txt

# Internal Reference
.claude/
